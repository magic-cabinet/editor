# Optional environment variables

# Dev server port (default: 3002, set in .env.defaults)
# PORT=3002

# Required only by the Magic Cabinet Docker stack. Generate a fresh value with:
# openssl rand -hex 32
# PASCAL_MCP_HTTP_TOKEN=

# Docker host port (loopback only; default: 8080)
# MAGIC_CABINET_PORT=8080
